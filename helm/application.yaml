apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fitness-tracker-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Suryaa11/Fitness_Tracker.git
    targetRevision: test
    path: helm
    helm:
      values: |
        userprofile:
          activeVersion: blue
  destination:
    server: https://kubernetes.default.svc
    namespace: fitness-ns
  syncPolicy:
    automated:
      prune: true
      selfHeal: true